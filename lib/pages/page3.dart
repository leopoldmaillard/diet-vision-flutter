import 'package:flutter/material.dart';

class Page3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "Meal history page. The user will be able to save it's meal and the associated results and review them later in this tab.",
        style: new TextStyle(fontSize: 20.0),
      ),
    );
  }
}
